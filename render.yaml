
services:
  - type: web
    name: hydronexus
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hydronexus-db
          property: connectionString
    healthCheckPath: /api/health

databases:
  - name: hydronexus-db
    plan: free
